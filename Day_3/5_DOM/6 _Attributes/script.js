// .attributes

// .hasAttribute -true- false return

// .getAttribute('attrName')

// .setAttribute('attrName', 'value')
//Even if that element has no that attribute a new attribute is creating for it

// .removeAttribute('attrName');
//Note: even the class/ id attributes so you can play with em with this method
